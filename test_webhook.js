const http = require('http');

async function run() {
  // We can't easily start next.js just for this, but we can look at the code.
  // Next.js NextResponse with a string body returns text/plain by default.
}
run();
